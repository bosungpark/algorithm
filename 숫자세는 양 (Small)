import sys

T=int(sys.stdin.readline())

for x in range(1,T+1):
    N=int(sys.stdin.readline())

    sheeps=set()
    if N==0:
        print(f'Case #{x}: INSOMNIA')
        continue
    i=0
    while len(sheeps)!=10:

        i+=1
        y=str(i*N)          
        for num in y:
            sheeps.add(num)
        
    print(f'Case #{x}: {y}')
